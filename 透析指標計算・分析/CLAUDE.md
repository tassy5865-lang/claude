# 透析指標計算・分析

透析患者の1回の治療データから、除水・循環動態・透析効率・貧血/電解質・心機能の各指標を自動計算し、
AI（Claude等）へ医学的考察を依頼するためのプロンプト文をExcel関数で自動生成する臨床用計算ツール。

- 元データ: `透析指標計算・分析.xlsx`（コードなし、Excel関数のみで構成。オリジナル仕様のリファレンス）
- Web版: `index.html`（HTML/CSS/JS単一ファイル、フレームワーク不使用・ビルド不要。ブラウザで開くだけで動作）。[[project_tetris]]や[[project_dragon_quest]]、[[project_taretool]]と同じくビルドレス・ブラウザ完結構成
- 対象読者: このフォルダで作業する将来のClaude — シートを都度バイナリ解析しなくて済むよう、シート構成・主要数式・変数の意味をここにまとめる

## index.html（Web版）の実装状況

**実装済み（v1・コア機能のみ）:** xlsxシート1「除水設定,循環動態管理」＋シート2「透析効率,貧血管理」の計算ロジックを移植。
- 基本情報・体重・血液データの入力 → ΔBW・体重変化率・TP濃縮率・UFR・π1/π2(膠質浸透圧)・CPV1/CPV2(循環血漿量)・CPV Rate・**PWI**・**mean Kr** をリアルタイム自動計算、適正範囲外は判定バッジ表示
- URR・**Kt/V (Daugirdas)** の自動計算と目標値（65%/1.2）バッジ
- 現在の機器設定（Qb/Qd/Area）→ 実測Kt/Vから現在のKoAを逆算 → Michaels式で理論クリアランスを算出 → 実効効率係数
- 処方変更シミュレーション（変更後Qb/透析時間/KoA）→ 新理論K・新実測予測K・予測Kt/V、優先改善アクション・除水リスク判定を自動判定
- 貧血・電解質（Hgb/ESA/リン/カリウム）→ ERI・リン除去率(PRR)・カリウム除去率(KRR)、貧血管理アラート・リン除去アラートを自動判定
- 入力値は`localStorage`（キー`dialysisCalc_v1`）に自動保存し次回起動時に復元

**実装済み（AI相談用プロンプト自動生成、2026-07-26追加）:** 「AI相談用プロンプト自動生成」カード（page下部）に5種類のプロンプトを常時ライブ生成（入力するたびtextareaが更新）、各ブロックの「📋 コピー」ボタンでクリップボードにコピー（`navigator.clipboard`、失敗時は`execCommand('copy')`にフォールバック）、右下トースト通知で完了を表示。
1. 除水設定・循環動態の考察依頼（xlsx Sheet1 A13相当）
2. 透析効率とのバランス考察依頼（xlsx Sheet1 A15相当）
3. 透析処方最適化レポート依頼（xlsx Sheet2 A15相当）
4. 処方シミュレーション・コンサルテーション依頼（xlsx Sheet2 A16相当）
5. 総合カンファレンス評価依頼（xlsx Sheet2 A26相当。元は「5ドメイン」と書かれていたが実際は4ドメイン分の項目しかなく、心不全管理シートは未移植のため「4ドメイン」に訂正して実装）

プロンプト④のため「変更後の膜面積」入力欄（`areaNew`）をシミュレーションセクションに追加済み。

**実装済み（PWI・mean Kr 経時グラフ、2026-07-26追加）:** xlsx Sheet3相当。日付＋その時点のPWI/mean Kr/UFRを記録する運用（`logDate`＋「この結果を記録する」ボタン→`localStorage`キー`dialysisCalc_trend_v1`に配列で保存、同日再記録は上書き）。記録一覧テーブル（削除可）と、外部ライブラリ不使用の自作SVG折れ線グラフ（`buildLineChartSVG()`）でPWI・mean Krそれぞれの推移を表示、適正範囲（PWI 2〜4 / Kr 1〜4）を網掛け表示。

**実装済み（心不全管理、2026-07-26追加）:** xlsx Sheet4相当。
- LVEF/LVDd入力 → HFrEF(<40%)/HFmrEF(<50%)/HFpEF分類・正式名称・収縮能評価・推奨治療薬概要を自動判定
- 壁運動スコアリング：16区域（元xlsxもA8:A23の16区域のみで、標準17区域中の心尖部キャップは元データに無いため踏襲せず）を`<select>`（1点Normal〜4点Dyskinesis）で評価、`WALL_SEGMENTS`配列からテーブルを動的生成、WMSIは平均値
- 弁膜症評価：MR/AR/AS/TRを None/Trivial/Mild/Moderate/Severe から選択（元xlsxの入力規則リストは順不同だったため臨床的な重症度順に並べ替え）
- 解析結果コメントを自動合成（EF分類＋収縮能評価、WMSI>1.2でRWMA疑いコメント、LVDd≧55mmでリモデリング示唆コメント、Severe弁膜症があれば増悪因子コメントを追加）
- 心不全データが揃ったことで、プロンプト⑤（総合カンファレンス評価依頼）に「■5. 心機能」ドメインを追加し、見出しも「4ドメイン」から本来の**「5ドメイン」に復元**した

**CSV出力機能（2026-07-27追加）:** タブバー直下に常時表示の「📥 現在のデータをCSVダウンロード」ボタンと、経時グラフタブの記録一覧に「📤 記録をCSVダウンロード」ボタンを追加。
- 現在データ出力: `exportSnapshot()`が入力中の全項目（基本情報〜心不全管理）と計算結果（PWI/Kr/URR/Kt/V/ERI/PRR/KRR/WMSI等）を「項目,値」の2列CSV（1行=1透析セッション相当）で書き出す。計算結果は`recalc()`末尾で`lastResults`に集約したものを参照（DOM再取得ではなく計算済み変数を直接使う）。
- 経時記録出力: `exportLog()`が`dialysisCalc_trend_v1`（PWI/mean Kr/UFRの日次記録）をそのままCSV化。
- 共通実装: `downloadCSV()`がBlobに先頭BOM(`﻿`)を付与しExcelでの日本語文字化けを防止、`csvEscape()`でカンマ・改行・ダブルクォートを含む値をクオート、ファイル名は`timestampForFilename()`で`YYYYMMDD_HHmm`を付与。外部ライブラリ不使用。

**印刷用レイアウト（2026-07-27追加）:** CSV出力ボタンの隣に「🖨 印刷する」ボタンを追加。クリック時に出力日時を`.print-header`（画面上は非表示、印刷時のみ表示）へ埋め込んでから`window.print()`を呼ぶ。`@media print`で以下を制御:
- `.no-print`クラス（タブバー・CSV/印刷ボタン欄・経時グラフの記録操作欄・記録一覧の削除ボタン列）を非表示
- `.container section[data-tab]`を`display:block !important`にして現在選択中のタブに関わらず全タブを縦に並べて出力。ただし`section[data-tab="ai"]`（AIプロンプトタブ）だけは印刷対象外として明示的に非表示に戻す（大量のtextareaを紙に出す意味が薄いため除外）
- カードの影を消し枠線に変更、バッジに枠線を追加するなど白黒印刷向けの見た目調整
claude-in-chromeで実機検証済み（`window.print`を一時的に上書きしてOSダイアログを開かずにハンドラの動作を確認、スタイルシートのprintメディアルールと`.no-print`要素数をJS経由で検証）。

**複数患者管理（2026-07-27追加）:** 患者バー（タブバー上部）に患者切替<select>・✏️名前変更・＋新規患者・🗑削除を追加。`window.prompt()`/`confirm()`は使わず、既存のトースト通知と同じ設計思想でインライン展開パネル（`#patientPanel`、テキスト入力または確認メッセージ＋OK/キャンセル）で名前入力・削除確認を行う。
- ストレージ設計: 患者一覧は`dialysisCalc_patients_v1`（`{id,name}`配列）、現在選択中の患者IDは`dialysisCalc_currentPatientId_v1`。各患者の入力データ・経時記録は`dialysisCalc_v1_<患者id>`・`dialysisCalc_trend_v1_<患者id>`という患者ごとのキーに保存（旧単一患者版のキー`dialysisCalc_v1`/`dialysisCalc_trend_v1`は初回起動時に自動マイグレーションし「患者1」として引き継ぐ）
- 患者切替時は必ず`clearForm()`（HTML初期値へリセット）→ 既存患者なら`load()`で上書き、の順で処理する。`load()`は値が空文字のフィールドを上書きしない仕様のため、先にクリアしないと前の患者の入力値が新しい患者の空欄に残ってしまう（レビューで発見し、切替のたびに必ずclearFormしてからloadする設計に統一）
- 削除処理の順序に注意: `switchPatient()`内部の`save()`/`saveLog()`は関数の先頭で「切替前の（＝削除対象の）患者ID」宛にまだ書き込むため、`localStorage.removeItem`を先に呼ぶと`switchPatient()`が直後に同じキーを復活させてしまう。そのため削除処理は「patients配列から除外→`switchPatient()`で次の患者に切り替え→その後にlocalStorageから削除」の順で実装（実装中に気づき修正）
- 最後の1人は削除不可（トーストで案内）
- CSV出力（項目に「患者名」を追加）・印刷ヘッダー（「患者: 〇〇 ／ 出力日時: …」）にも患者名を反映、CSVファイル名にも患者名を含める
claude-in-chromeで実機検証済み: 患者追加→フォームが初期値にクリアされる／既存患者データが別患者切替後も相互に汚染されない／名前変更／削除でlocalStorageキーが実際に消えること（JS経由で`localStorage.getItem`が`null`になることを確認）／最後の1人削除ガード／CSV・印刷への患者名反映、を一通りクリックとJS検証の両方で確認。コンソールエラーなし。

**心不全管理タブのアコーディオン化（2026-07-27追加）:** 16区域の壁運動スコアリング表と弁膜症評価表（4行）が縦に長く場所を取っていたため、それぞれネイティブ`<details>/<summary>`でアコーディオン化（デフォルト折りたたみ）。JS実装は不要（ブラウザ標準の開閉動作をそのまま利用）。折りたたんだ状態でも一目でわかるよう、`<summary>`内に常時サマリー値を表示:
- 壁運動スコアリング → `<summary>`にWMSI値（既存`#out-wmsi`をそのままsummary内に移動、recalc()側の変更不要）
- 弁膜症評価 → `<summary>`に新設の`#out-valve-summary`（「異常なし」／「軽度〜中等度N件」／「⚠ Severe N件」を`recalc()`内で判定）
印刷時は`details.section-details:not([open]) > *:not(summary) { display:block !important; }`で強制的に中身を展開表示するよう`@media print`に追記済み（折りたたんだまま印刷すると壁運動・弁膜症の詳細が紙に出ない、という抜け漏れを事前に潰した）。
claude-in-chromeで実機検証済み: 折りたたみ⇄展開のクリック動作、WMSIサマリーが壁運動スコア変更で即時再計算（1.00→1.13）、弁膜症サマリーがSevere選択で「⚠ Severe 1件」に即時更新、コンソールエラーなし。

**デザインのモダン化（2026-07-27追加）:** 機能追加ではなく既存UIの見た目のみを刷新（構造・class名・id・JS動作は変更なし）。
- カラーパレット刷新: accent を `#2563a8`→`#2f6fed`（より鮮やかな青）に変更、`--radius-sm/md/lg`・`--shadow-sm/md`のデザイントークンを`:root`に追加
- カード見出し(h2)を「青文字＋下線」から「ニュートラル文字色＋左に色付きアクセントバー(4px)」に変更（`h2::before`で実装）
- バッジに色付きドット（`::before`、`background:currentColor`でok/warn/cautionそれぞれの色を自動継承）を追加
- ボタン類（`.btn-primary`/`.copy-btn`）にホバー時の浮き上がり（`translateY(-1px)`+シャドウ拡大）を追加、`.action-box`はベタ塗りから左ボーダーアクセント付きカードに変更
- カード角丸を12px→18px（`--radius-lg`）に拡大、シャドウを二段階に整理してより柔らかい印影に
- `<details>`アコーディオンのsummaryにホバー時のハイライト背景を追加
- タブ・入力欄・select等のフォーカス時に`:focus-visible`のアクセシビリティ用アウトラインを追加
- 経時グラフのSVG折れ線チャートも新配色（`#2f6fed`）に追従、ポイントに白縁取り・線の角を丸く（`stroke-linecap/linejoin: round`）して質感を向上
- 🗑削除ボタン（患者削除）はホバー時のみ警告色（赤系）に変化するよう追加（誤操作防止の視覚的ヒント）
印刷用CSS（`@media print`）は意図的に据え置き（白黒印刷前提のシンプルな配色のまま）。
claude-in-chromeで実機確認済み: 全タブの見た目、バッジのドット表示、ボタンのホバー効果、経時グラフの新配色、コンソールエラーなしを確認。

**バグ修正（2026-08-02、バグ・不具合の点検依頼で発見）:** claude-in-chromeで実データ入力（正常値）と異常値の両方を実機テストし、以下1件の実質的なバグを発見・修正。
- **透析後体重が透析前体重を上回る入力（体重欄の前後を取り違えた場合など）でPWIが負の値になるのに、バッジが臨床的に確信ありげな誤った助言（「DWを下げる必要がある」等）を表示していた。** 例: 透析前65.5kg→透析後63.0kgの正常値ではPWI=4.367（適正な警告）だが、意図的に透析前60kg/透析後61kg（体重が増加）にするとPWI=-4.286という物理的に無意味な値になり、それでも`pwiJudge()`/`krJudge()`は通常のしきい値判定（`pwi<2`等）をそのまま適用し「DWを下げる必要がある」と表示していた。修正: `recalc()`で`weightAnomaly = isFinite(dBW) && dBW < 0`を判定し、`pwiJudge(pwi, weightAnomaly)`/`krJudge(meanKr, weightAnomaly)`/除水リスク判定の`pwiJudge(pwi, weightAnomaly)`（3箇所）に伝播、該当時は「透析後体重が透析前体重を上回っています（入力値を確認してください）」という専用メッセージ（warn）を優先表示するよう変更。
- 上記と連動し、**経時グラフ（`buildLineChartSVG()`）はy軸下限を`0`に固定していたため、負のPWI値を記録するとグラフ範囲外（SVG viewBox外）にクリップされ、点が完全に非表示になる**という副次バグもあった。修正: `yMin = Math.min(0, Math.floor(Math.min.apply(null, values)) - 1)`とし、負の値があれば動的にy軸を下に拡張するよう変更（通常の正の値のみの場合は従来通り`yMin=0`のまま、既存表示に影響なし）。
claude-in-chromeで正常値・異常値の両方を実機検証: 正常値（体重減少）ではバッジ・グラフとも修正前と同じ挙動を維持（回帰なし）、異常値（体重増加、PWI=-4.286）では新しい警告メッセージが表示され、経時グラフのy軸も自動的に負の範囲まで拡張されて点が正しく可視化されることを確認。コンソールエラーなし。

**経時グラフの拡張（2026-08-02追加）:** 従来はPWI・mean Kr・UFRの3指標のみ日付付きで記録可能だったが、Kt/V・URR・ERI・体重(DW)の4指標を追加し、計7指標＋UFRを経時記録・グラフ化できるようにした。
- `recalc()`で新たに`currentKtv`/`currentUrr`/`currentEri`/`currentBwPost`をグローバル変数として保持（既存の`currentPWI`/`currentMeanKr`/`currentUFR`と同じパターン）、`addLogEntry()`はこれら7項目のうち**いずれか1つでも計算できていれば記録可能**に緩和（従来はPWI・mean Kr両方必須だった）。
- 記録一覧テーブルに列を追加（日付/PWI/mean Kr/UFR/Kt/V/URR/ERI/体重(DW)、計8列+削除ボタン）。列数が増えたため`<table>`を`overflow-x:auto`のdivで囲み、狭い画面でもページ全体が横スクロールしないようにした（ユーザーが別候補として挙げていた「記録一覧テーブルの表示改善」そのものではなく、列増加に伴う最低限のレスポンシブ対応として実施）。
- `buildLineChartSVG()`を拡張し、PWI/mean Kr/ERIのような「範囲で示す指標」（`refMin`/`refMax`で網掛け帯）に加え、Kt/V・URRのような「◯◯以上が目標」という片側指標用に`refLine`（破線の目標ラインとラベル）を描画するモードを追加。体重(DW)のようにそもそも臨床的な目標帯がない指標は`refMin`/`refMax`/`refLine`いずれも省略すれば帯もラインも出さずに折れ線のみ表示。
- 体重(DW)チャートは他の指標と異なり`zeroBase:false`を指定し、y軸下限を0固定ではなく実測値の範囲（floor(min)-1）から始めるようにした。体重は通常60〜70kg等の狭い帯で推移するため、他指標と同じ0始まりだと変化がほぼ見えないグラフになってしまうための個別対応（PWI/mean Kr/Kt/V/URR/ERIは従来通り0始まり＝`zeroBase`省略時のデフォルト）。
- CSV出力（`exportLog()`）も新4列に対応し、ファイル名を「PWI_Kr経時記録_」から「経時記録_」に変更（内容が指標横断になったため）。
- 旧形式（`ktv`/`urr`/`eri`/`bwPost`キーを持たない）の既存記録データはlocalStorageにそのまま残っていても、`isFinite(undefined)`が`false`になるため新列は自動的に「—」表示となり、マイグレーション処理不要でそのまま動作することを実機確認済み。
claude-in-chromeで実機検証済み: 新4指標を含む記録の追加・テーブル表示・6種類全チャートの描画（PWI/Kr=網掛け帯、Kt/V/URR=破線目標ライン、ERI=網掛け帯、体重=ズームされた自動レンジ）・CSVダウンロード内容・旧形式データとの後方互換性、いずれもコンソールエラーなしで確認。

**記録一覧テーブルの日付列固定（2026-08-02追加）:** 経時グラフの記録一覧テーブルが8列（日付/PWI/mean Kr/UFR/Kt/V/URR/ERI/体重(DW)）に増え横スクロールが必要になったため、`.log-table-wrap`（`overflow-x:auto`のラッパー、旧`overflow-x:auto`のインラインstyle divをclass化）内で`.log-table th:first-child`/`.log-table td:first-child`に`position:sticky; left:0;`を指定し、横スクロールしても日付列が常に左端に固定表示されるようにした。固定列にはカード背景色＋右側にうっすら影（`box-shadow: 2px 0 4px rgba(...)`）を付け、スクロール中のコンテンツと視覚的に区別できるようにしている。行ホバー時のハイライト（`.mini-table tbody tr:hover`）は通常のtdには効くが、position:stickyのtdは別レイヤーになるため`.log-table tbody tr:hover td:first-child`を追加でホバー時に日付セルの背景も揃うようにした。このスタイルは`.log-table`クラスを持つテーブルにのみ適用され、心不全管理タブの壁運動・弁膜症テーブル（`.mini-table`のみでスクロールもしない）には影響しない。
claude-in-chromeでウィンドウ幅を絞った状態を再現し検証（wrapのmax-widthを一時的に狭めてscrollLeftを動かし、`getComputedStyle`で`position:sticky`が適用されていること、実際に横スクロールしても日付列が画面に残ることをスクリーンショットで確認）。コンソールエラーなし。

**ダークモード対応（2026-08-02追加、以前は「頼まれていない機能」としてスコープ外にしていたが今回ユーザーが明示的に依頼）:**
- CSS変数を`@media (prefers-color-scheme: dark)`（OS/ブラウザ設定に自動追従）と`:root[data-theme="dark"]`（手動トグルによる明示的な上書き）の2系統で二重定義。`:root:not([data-theme="light"])`というセレクタにすることで、「システムがdarkだが明示的にlightを選んだ」場合にmedia query側を無効化できるようにしている。
- 右上固定の🌙/☀️トグルボタン（`#themeToggleBtn`、`no-print`）で手動切替。選択結果は`localStorage`（`dialysisCalc_theme_v1`）に保存し、`<head>`内の`</style>`直後に置いた同期的なインラインスクリプトで**CSS適用前に**`data-theme`属性をセットすることで、リロード時に一瞬ライトモードが見えてしまうFOUC（Flash of Unstyled Content）を防止している。
- **印刷は明示的に`@media print`内で`:root`の全CSS変数をライトモード値に強制上書き**し、画面がダークモードでも印刷結果は常に白背景・黒文字になるようにした（[[project_dialysis_tool]]のデザイン刷新時に「実装するとしたら印刷用CSSでの上書きが必要」と記録していた設計注意点をそのまま反映）。claude-in-chromeで`data-theme="dark"`のまま`document.styleSheets`経由で`@media print`内`:root`の`--card-bg`を検査し`#ffffff`に上書きされていることを確認済み。
- ネイティブフォームコントロール（日付ピッカーのカレンダーアイコン等）もテーマに追従するよう、3箇所の変数ブロックそれぞれに`color-scheme: light` / `color-scheme: dark`を追加。
- **既存コードの中で見つかった実質的なダークモード対応漏れを修正:** AIプロンプトタブの`textarea.prompt-textarea`の背景色がハードコード`#fafcff`（ほぼ白）のままで、文字色だけ`var(--text)`を使っていたため、ダークモードでは「明るい背景に明るい文字」でほぼ読めなくなるバグがあった。`background: var(--bg)`に変更して修正（[[project_dialysis_tool]]で確立している「新パレット導入時はハードコードされた色を全て洗い出す」という方針どおりに発見）。
- 経時グラフの自作SVGチャート（`buildLineChartSVG()`）内の軸線・軸ラベル・データポイントの縁取りもハードコード16進色（`#dfe5ef`, `#647089`, `#fff`）だったため、それぞれ`var(--border)`, `var(--text-sub)`, `var(--card-bg)`に置き換え、チャートもテーマに追従するようにした（データ系列自体の色は視認性の高い彩度の高い色のため、両テーマで据え置き）。
claude-in-chromeで実機検証済み: ブラウザのシステム設定がダークだったため未操作の状態で自動的にダークモードで表示されることをまず確認、その後トグルクリックでライトへ切替→リロードしてFOUCなく設定が保持されることを確認、AIプロンプトのtextarea・バッジ・トースト・経時グラフ全種・削除確認パネルの見た目を両テーマで確認、印刷用CSSの変数上書きをJS経由で検証。コンソールエラーなし。

**貧血評価のガイドライン準拠化＋TSAT追加（2026-08-02追加、ユーザーの明示依頼）:** 従来はERI単独判定だった貧血管理アラートを、**日本透析医学会「2015年版 慢性腎臓病患者における腎性貧血治療のガイドライン」**の基準に沿って再設計。ユーザーへの事前確認（AskUserQuestion）で参照ガイドライン・TSATの入力方法・フェリチン追加の要否を確認した上で実装。
- **新規入力項目:** 血清鉄(Fe, μg/dL)・TIBC(総鉄結合能, μg/dL)・フェリチン(ng/mL)を貧血・電解質タブに追加（`fe`/`tibc`/`ferritin`、`ids`配列・save/load/CSV/AIプロンプトすべてに反映済み）。
- **TSAT算出:** `TSAT = Fe ÷ TIBC × 100`（%）。既存のERI算出と同じ「元データから逆算」の設計方針を踏襲し、TSATを直接入力させず血清鉄とTIBCから計算する方式にした（ユーザーが直接入力方式より血清鉄/TIBCからの自動計算を選択）。
- **貧血管理アラート（Hgb目標値ベースに刷新）:** HD患者のHgb目標「10≦Hgb<12 g/dL」に基づき、Hgb<10で「ESA開始・増量検討」、Hgb≧12で「ESA減量・休薬検討」、範囲内なら「目標範囲内」を表示。ERI>15（従来からの閾値を維持）の場合は別文で「ESA低反応性が疑われます」を追記——1つの`if-else`分岐ではなく、該当する文をすべて配列に積んで`join(" ")`する方式にし、Hgb評価とERI評価が同時に成立するケース（例: Hgb低値かつERI高値）でも両方のメッセージが欠落しないようにした。
- **鉄補充適応アラート（新設の第3のaction-box）:** ガイドラインの「TSAT≦20%かつフェリチン≦100 ng/mLで鉄補充適応」「フェリチン>300 ng/mLで鉄過剰リスクのため鉄補充回避」をそのまま実装。フェリチン>300の回避基準を最優先で判定し（TSATの値によらず鉄過剰リスクを優先表示）、次に両方の適応基準を満たすかどうかを判定、いずれの基準にも該当しない場合は「概ね良好」、TSAT・フェリチンの一方のみ入力の場合は判定保留の文言を出す4分岐構成。
- **AI相談プロンプト⑤（総合カンファレンス）の■3.貧血管理セクション**にTSAT・フェリチンの値を追記。
- CSV出力（現在データ）にも血清鉄・TIBC・フェリチン・TSATの4項目を追加。
claude-in-chromeで実機検証済み: (1)鉄欠乏で補充適応となるケース（TSAT12%・フェリチン80）、(2)フェリチン過剰で回避となるケース（フェリチン350、TSAT16%でも回避が優先される）、(3)Hgb高値かつ鉄代謝正常なケース（Hgb12.5・TSAT33%）の3パターンで貧血アラート・鉄補充アラートの文言が想定通り出ることを確認、CSV・AIプロンプトへの反映、リロード後のデータ永続化も確認。コンソールエラーなし。

**CKD-MBD（骨・ミネラル代謝異常）タブ新設＋薬剤投与量分析（2026-08-02追加、ユーザーの明示依頼「PTHやP,Caなどの評価もガイドラインをもとに追加して、パーサビブやウパシタ、オキサロールなどの薬剤投与量の分析も」）:** 貧血評価の時と同様、医療系の数値基準を伴う機能のため実装前にAskUserQuestionで(1)参照ガイドライン、(2)Ca評価方式（実測 vs 補正）、(3)薬剤投与量分析の粒度、の3点を確認してから着手。
- **新規タブ「🦴 骨・ミネラル代謝」**（`data-tab="mbd"`）を貧血・電解質タブと心不全管理タブの間に追加。タブバーは既存の`overflow-x:auto`によりタブ数が増えても横スクロールで自然に収まる（7タブ目でも追加のCSS変更は不要だった）。
- **日本透析医学会「2012年版 慢性腎臓病に伴う骨・ミネラル代謝異常の診療ガイドライン」**の目標値を採用: P 3.5〜6.0 mg/dL・補正Ca 8.4〜10.0 mg/dL・intact PTH 60〜240 pg/mL、管理優先順位はP→Ca→PTH。
- **P（リン）は新規入力欄を作らず、既存の貧血・電解質タブにある「透析前 リン」(`pPre`、PRR計算にも使う値)をそのまま参照する設計にした**（同じ臨床概念の値を2箇所に重複入力させない）。
- **補正Ca:** 実測Ca(`caRaw`)とアルブミン(`alb`)からPayne式で自動計算（`Alb<4のときのみ 実測Ca+(4-Alb)`、Alb≧4またはAlb未入力なら実測Caをそのまま使用）。
- 汎用のレンジ判定ロジックを`rangeJudge(v, lo, hi, lowText, highText)`として`pwiJudge`/`krJudge`と同じ場所に切り出し、P/Ca/PTHの3指標のバッジ判定に共通利用（コード重複を避けつつ既存のバッジ表示パターン(ok/warn/caution)を踏襲）。
- **CKD-MBD管理アラート:** P・Ca・PTHそれぞれが目標範囲外の場合にメッセージを配列に積んで結合する方式（[[project_dialysis_tool]]の貧血アラートで確立した「複数の該当条件を漏らさず全部表示する」パターンを踏襲）。P異常時は「管理の優先順位はP→Ca→PTH」という文言を添えて優先度を明示。
- **薬剤投与量の記録＋照らし合わせ表示（パーサビブ/ウパシタ/オキサロール）:** 各薬剤の1回投与量を入力すると、投与中の薬剤だけについて「カルシミメティクス(パーサビブ/ウパシタ)はPTH・Caをともに低下→Ca低値なら減量/休薬を検討」「活性型VD3製剤(オキサロール)はPTHを下げるがCa・Pを上げうる→P/Ca高値なら増量注意」という薬理学的な一般原則を提示する設計。**あえて具体的な増減量（mg数など）は提示しない**——ユーザーが選択したのは「用量記録＋P/Ca/PTHとの照らし合わせ表示」であり「AIプロンプトのみに任せる」ではなかったため一般原則の文章化は行ったが、薬剤ごとの滴定量提案は臨床判断の自主性が強い領域のためツール側では踏み込まない、という線引きをした。
- AI相談プロンプト⑤（総合カンファレンス）に**■6. 骨・ミネラル代謝(CKD-MBD)**ドメインを追加し、見出しも「5ドメイン」→「6ドメイン」に更新（[[project_dialysis_tool]]で心不全管理追加時に確立した「新ドメイン追加時はプロンプトのドメイン数表記も必ず更新する」という運用を踏襲）。
- CSV出力にもP・実測Ca・アルブミン・補正Ca・intact PTH・3薬剤の投与量を追加。
claude-in-chromeで実機検証済み: P高値+PTH高値+オキサロール投与中のケース、Alb正常でCa補正が働かないケース、PTH低値+パーサビブ/ウパシタ投与中のケースの3パターンでバッジ・アラート文言・薬剤照らし合わせ文言が想定通りになることを確認。7タブ全てを`.tab-btn`クリックで一巡し表示切替に問題がないことも確認（既存タブの回帰なし）。CSV・AIプロンプトへの反映、リロード後の永続化も確認。コンソールエラーなし。

**未実装（必要になれば追加）:** 現時点でxlsxの4シートは全てWeb化済み＋CSV出力・印刷レイアウト・複数患者管理・心不全管理タブのアコーディオン整理・モダンデザイン刷新・経時グラフの指標拡張・記録一覧テーブルの日付列固定・ダークモード対応・貧血評価のガイドライン準拠化(TSAT追加)・CKD-MBDタブ(P/Ca/PTH+薬剤投与量分析)まで完了。追加候補があれば都度相談。

**重大バグ修正（2026-07-27、ユーザー報告「貧血・電解質、心不全管理にデータを入力するところがない」）:** `setTab()`が非アクティブタブを`display:none`、アクティブタブを`display:""`（空文字＝インラインスタイル解除）で切り替えていたが、CSS側に`.container section[data-tab]:not([data-tab="input"]) { display: none; }`という初期非表示ルールがあり、インラインスタイルを空文字で「解除」すると結局このCSSルールが効いて`display:none`のままになっていた。この結果、**「input」タブ以外（efficiency/anemia/heart/trend/ai）はどれをクリックしても中身が表示されない**という重大な不具合が発生していた（貧血・電解質・心不全管理に限らず全タブが影響対象）。`sec.style.display = (match) ? "block" : "none";`と明示的な値に変更し、インラインスタイルの優先度でCSSルールを確実に上書きするよう修正。

**バグ修正（2026-07-27、コードレビューで発見）:**
- URR・Kt/Vのバッジが、まだ何も入力していない初期状態でも赤い「目標未達」警告を表示していた（PWI/mean Krバッジは`入力不足`という中立表示があったのに、URR/Kt/Vだけ抜けていた）。`isFinite`チェックを追加し、未入力時は他と同じ`caution`（中立）表示にした。
- 心不全管理の「推奨治療薬の概要」（例:「Fantastic Four (ARNI/ACEi/ARB, β遮断薬, MRA, SGLT2i)」）が`.out-row .value`の`white-space:nowrap`を継承して折り返されず、カード幅からはみ出す可能性があった。該当要素に`white-space:normal; overflow-wrap:anywhere;`を追加して修正。
- 貧血・電解質管理／心不全管理セクション内の2列レイアウトが、他のカードと違って画面幅に関わらず常時2列固定（インラインstyle）になっていた。`.two-col-grid`クラスに切り出し、`@media (min-width:640px)`で2列化・それ未満は1列に落ちるよう修正（狭い画面でのレイアウト崩れ対策）。

上記の重大バグ（タブ表示）と3件の軽微バグは、claude-in-chromeスキルでローカルサーバー（`python -m http.server`）経由でブラウザを開き、全6タブのクリック・LVEF入力によるHFrEF分類・URR/Kt/Vバッジ表示を実際に操作して修正を確認済み（コンソールエラーなし）。

**タブ分けUI（2026-07-26追加）:** 全カテゴリが1ページ縦長スクロールになり見づらくなったため、上部にタブバー（💧除水・循環動態 / ⚙️透析効率・処方 / 🩸貧血・電解質 / ❤️心不全管理 / 📈経時グラフ / 🤖AIプロンプト）を追加。各`<section class="card">`に`data-tab`属性を付与し、`setTab()`が該当タブ以外の`section`を`display:none`で非表示にする方式（新しいラッパーやiframeは使わず、既存のグリッドレイアウト・IDはそのまま）。選択中タブは`localStorage`(`dialysisCalc_tab`)に保存し次回起動時も復元。CSSの`.container section[data-tab]:not([data-tab="input"])`で初期表示（JS実行前）から非inputタブを隠しちらつきを抑制。

**xlsxとの既知の差異（意図的な修正）:**
- xlsx側のERI計算式（Sheet2 F18）は空白セル（B5）を分母に参照しており、元データでは常に`#DIV/0!`になるバグがあった。Web版では臨床的に妥当な計算式（ESA週間投与量 ÷ 透析後体重(DW) ÷ Hgb）に修正して実装している。
- xlsx側の総合カンファレンス評価依頼（Sheet2 A26）は「Kt/V」ラベルに実測クリアランス値（F3、ml/min単位、~248等）を誤って参照しており、Kt/Vの数値として意味をなさないバグがあった。Web版では実測Kt/V（0.93等の比の値）を正しく参照して実装している。

## 全体構成（4シート）

1. **除水設定,循環動態管理** — 基本情報・体重・血液データを入力し、PWI/mean Kr等の循環動態指標とKt/V(Daugirdas)/URRを算出
2. **透析効率,貧血管理** — Michaels式によるダイアライザ理論クリアランスとQb/KoA変更シミュレーション、貧血(ERI)・電解質(P/K除去率)を算出
3. **PWI.Krグラフ** — シート1のPWI・mean Krを日付ごとに記録してグラフ化する経時トレンドシート
4. **心不全管理** — LVEF分類(HFrEF/HFmrEF/HFpEF)、17区域壁運動スコアリング(WMSI)、弁膜症評価

シート間参照が多いため、**シート名を変更するとリンク切れ（`'除水設定,循環動態管理'!B2`のような式）が起きる**点に注意。

## シート1: 除水設定,循環動態管理

### 入力セル
- B2 透析時間(min) / B3 総除水量(ml)
- B5 透析前体重 / B6 透析後体重(kg)
- B8 透析前TP / B9 透析後TP(g/dL)
- B10 透析前Ht(%)
- B11 透析前BUN(Co) / B12 透析後BUN(Ct)(mg/dL)

### 算出指標と数式
| 指標 | セル | 数式 | 意味 |
|---|---|---|---|
| ΔBW(除水量) | F2 | `B5-B6` | 単純体重差 |
| 体重変化率 | F3 | `F2/B5` | ΔBW/PreBW |
| TP濃縮率 | F4 | `1-(B8/B9)` | |
| UFR(除水速度) | F5 | `B3/B2` | ml/min |
| π1/π2(膠質浸透圧) | F6/F7 | `2.1*TP + 0.16*TP^2 + 0.009*TP^3` | TP1/TP2をそれぞれ代入する3次多項式近似 |
| CPV1(循環血漿量 前) | F8 | `0.077*B5*1000*(1-B10/100)` | 体重の7.7%×(1-Ht) |
| CPV2(循環血漿量 後) | F9 | `F8*(B8/B9)` | CPV1×(TP1/TP2) |
| **PWI** | I2 | `F4/F3`（TP濃縮率÷体重変化率） | Protein-Water Index。**2≦PWI≦4が適正範囲**。<2はDW下げ要、>4はDW上げ要（血管内リフィル能の指標） |
| **mean Kr(プラズマリフィル係数)** | I3 | `(F5-I4)/(F7-F6)` | ml/min/mmHg。**1≦Kr≦4が適正範囲**。<1はDW上げ要、>4はDW下げ要 |
| CPV Rate(血漿量減少速度) | I4 | `(F8-F9)/B2` | ml/min |
| URR(除去率) | I7 | `((B11-B12)/B11)*100` | % |
| Kt/V(Daugirdas) | I8 | `-LN(Ct/Co - 0.008*(t/60)) + (4-3.5*(Ct/Co))*(ΔBW/BWpost)` | 標準Daugirdas式 |

下部（A13以降）にはB列の値からCHAR(10)で連結した**AI考察依頼プロンプト文**を自動生成するセルが2つある（除水・循環動態の考察依頼、透析効率とのバランス考察依頼）。数値を変えるとプロンプト文も自動更新される。

## シート2: 透析効率,貧血管理

### 現在の処方 → シミュレーション
- B2〜B9: 現在の透析時間/DW/実測Kt/V(シート1参照)/Qb/Qd/膜面積/現KoA
- B9 KoA逆算式: `(Qb/(1-Qb/Qd))*LN((1-K/Qd)/(1-K/Qb))`
- B11〜B14: シミュレーション用の変更後Qb/透析時間/KoA/膜面積
- F2 理論クリアランス: Michaels式 `Qb*(EXP(KoA/Qb*(1-Qb/Qd))-1)/(EXP(KoA/Qb*(1-Qb/Qd))-Qb/Qd)`
- F3 実測クリアランス: `Kt/V * (DW*0.6*1000) / t`（Kt/Vからの逆算）
- F4 実効効率係数: `実測K/理論K`（1.0から離れるほど再循環・目詰まり等のロス）
- F7/F8/F9: シミュレーション後の理論K/実測予測K/予測Kt/V
- I2/J2: 実効効率係数と実測Kt/V・Qbから「優先改善アクション」を自動判定（IF分岐: Kt/V≥1.2なら現状維持、効率係数<0.8なら再循環点検、Qb<250ならQb増量、それ以外は膜面積増）

### 貧血・電解質（B17以降）
- B18 Hgb / B19 ESA投与量週(IU/week)
- **ERI** F18: `ESA投与量/体重/Hgb` — 目安10〜15以上でESA低反応性（透析不足・鉄欠乏を疑う）
- B21/B22 透析前後リン、B23/B24 透析前後カリウム
- **PRR(リン除去率)** F19: `(P_pre-P_post)/P_pre*100` — 通常50〜60%程度
- **KRR(カリウム除去率)** F20: 同様の計算式

ここにも複数のAI考察依頼プロンプト（処方最適化レポート依頼、シミュレーション・コンサルテーション、5ドメイン統合の総合カンファレンス評価依頼）が自動生成される。C27〜C30はシート1の値をこのシート内に再掲するための参照セル。

**注:** TSAT（Fe/TIBC）・フェリチンおよびガイドライン準拠の貧血管理アラート・鉄補充適応アラートは元xlsxには存在せず、2026-08-02にWeb版のみに追加した項目（上記「貧血評価のガイドライン準拠化＋TSAT追加」参照）。

## シート3: PWI.Krグラフ

A/B列に日付とPWI、D/E列に日付とmean Kr（シート1参照）を追記していく経時データ表＋グラフ。新しい透析日のデータを追加する際はこの表に1行追加する運用。日付はExcelシリアル値。

## シート4: 心不全管理

- LVEF(B2)から`IF`分岐でHFrEF(<40%)/HFmrEF(<50%)/HFpEF分類、正式名称、推奨薬（Fantastic Four等）を自動判定
- 17区域の壁運動を1〜4点（Normal〜Dyskinesis）でスコアリングし、WMSI(C24)は`AVERAGE(C8:C23)`で平均算出。各区域には冠動脈支配（LAD/RCA/LCX）も対応付け
- 弁膜症(MR/AR/AS/TR)をNone〜Severeで評価
- A32セルがLVEF分類・WMSI・LVDd・弁膜症の重症度を組み合わせて自然文の解析結果コメントを自動合成する

## 運用上の注意

- 多くの指標セルはESA投与量やリン等が未入力だと`#DIV/0!`になる（意図した挙動。値を入れれば解消）
- シート名（`除水設定,循環動態管理`など、カンマを含む）は他シートから文字列参照されているため変更しない
- このワークブックの目的は「計算するだけ」でなく、算出した数値を埋め込んだ**AI相談用プロンプト文をワンクリックでコピーできる状態にする**ことが核。新しい指標を追加する際は、既存のプロンプト生成セルの書式（`&CHAR(10)&`で改行連結）に倣うと一貫性が保てる
- 実データ（体重・血液データ等）が含まれるファイルなので、内容を外部に共有・アップロードする際は患者情報の取り扱いに注意する
